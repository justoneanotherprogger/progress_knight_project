# AGENTS.md

Инструкции для ИИ-агентов, работающих в этом репозитории. Загружаются автоматически при сканировании проекта.

## Проект

Мод игры Progress Knight Quest (оригинал — https://indomit.github.io/progress_knight_2/): баланс, названия и контент — авторское видение владельца репозитория, не апстрима. Чистый JS без сборщика на фронте + Python-сборка контента.

Пайплайн сборки (`build.py`, Jinja2):

- `locales/<lang>/*.yaml` → `js/translations.js` (генерат, руками не править)
- `templates/*.html` и `templates/partials/*` → корневой `index.html` (генерат)

После изменения YAML или шаблонов запускать `python build.py` и коммитить обновлённые генераты вместе с источниками. Балансные правки — в исходниках (`js/*.js`, `locales/`), не в генератах.

## Структура

- `js/` — игровой код; `math.js` — вендорский бандл mathjs, не редактируется
- `templates/`, `locales/` — источники интерфейса и текстов
- `index.html` — собираемая точка входа
- `build.py` — генерация переводов и HTML из источников
- `scripts/` — служебные скрипты проверок

## Проверки перед коммитом

```sh
python scripts/check_file_size.py   # рэтчет размеров файлов
```

CI гоняет рэтчет на каждый push/PR (деплой в GitHub Pages — отдельным workflow); локальные pre-commit хуки включаются один раз на клон:

```sh
git config core.hooksPath hooks
```

## Сдерживание роста файлов

Исходники (`.js`, `.html`, `.css`, `.yaml`, `.py`) — до **1000 строк**. Переросшие исторически файлы запинены в `scripts/source-file-size-baseline.json` и могут только уменьшаться: любой рост запиненного файла отклоняется; похудевший требует понижения записи там же; дорос до лимита — запись удаляется. Вендорский `math.js` и генерат `translations.js` запинены на текущих значениях намеренно: их размер меняется только осознанным обновлением baseline.

Лимит, расширения и исключённые каталоги настраиваются в том же JSON — код чекера менять не нужно.
